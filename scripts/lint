#!/usr/bin/env bash
# Lint and format-check.
set -euo pipefail
cd "$(dirname "$0")/.."

RUFF=".venv/bin/ruff"
[ -x "$RUFF" ] || RUFF="ruff"
"$RUFF" check "$@" custom_components tests
"$RUFF" format --check custom_components tests
